# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build app1-bff`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t app1-bff`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system app1-bff && \
  adduser --system -G app1-bff app1-bff

COPY dist/apps/app1-bff app1-bff
RUN chown -R app1-bff:app1-bff .

WORKDIR /app/app1-bff

RUN yarn install --production --frozen-lockfile

CMD [ "node", "main" ]
