# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build oauth-agent-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t oauth-agent-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system oauth-agent-service && \
  adduser --system -G oauth-agent-service oauth-agent-service

COPY dist/apps/services/oauth-agent-service oauth-agent-service
RUN chown -R oauth-agent-service:oauth-agent-service .

WORKDIR /app/oauth-agent-service

RUN yarn install --production --frozen-lockfile

CMD [ "node", "main" ]
