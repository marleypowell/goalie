# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build goal-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t goal-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system goal-service && \
  adduser --system -G goal-service goal-service

COPY dist/apps/services/goal-service goal-service
RUN chown -R goal-service:goal-service .

WORKDIR /app/goal-service

RUN yarn install --production --frozen-lockfile

CMD [ "node", "main" ]
